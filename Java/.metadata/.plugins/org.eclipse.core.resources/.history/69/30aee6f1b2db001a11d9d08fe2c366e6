package backend;

import entity.CPU;

public class Exercise3 {
	
	public void question1(){
		CPU cpu = new CPU(100);
		CPU.Ram ram = cpu.new Ram(8, "HP");
		CPU.Processor processor = cpu.new Processor(10,"Broke");
		
	}
}
